server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
  instance:
    hostname: localhost
    prefer-ip-address: true

spring:
  application:
    name: eureka-server